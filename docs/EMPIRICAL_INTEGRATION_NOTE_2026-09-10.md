This branch began from `28e90af`. Box subsequently pushed executed hypothesis tests on main at `eaf2107`, including overlapping changes to the established-findings ledger. The certification work should therefore be replayed/reconciled onto a fresh branch from `eaf2107` rather than merged by overwriting either lane. PR #4 remains a preservation point until the reconciled branch is created.